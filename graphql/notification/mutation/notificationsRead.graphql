mutation notificationsRead(
  $id: String
  $recentToDeleteCount: Int
  $markAllAsRead: Boolean
) {
  notificationsRead(
    id: $id
    recentToDeleteCount: $recentToDeleteCount
    markAllAsRead: $markAllAsRead
  ) {
    message
  }
}
mutation login($email: String!, $password: String!) {
  login(
    input: {
      email: $email
      password: $password
    }
  ) {
    token
    twoFactorRequired
    challengeToken
    methods
    expiresIn
  }
}

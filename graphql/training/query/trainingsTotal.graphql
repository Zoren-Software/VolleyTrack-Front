query trainings(
    $filter: TrainingSearchInput
    $first: Int, 
    $page: Int
) {
  trainings(
    filter: $filter,
    first: $first,
    page: $page
  ) {
    paginatorInfo {
      total
    }
  }
}

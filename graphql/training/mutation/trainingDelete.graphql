mutation trainingDelete(
  $id: [ID!]!
) {
  trainingDelete(
    id: $id
  ) {
    id
    name
    userId
    user {
      id
      name
      email
    }
    teamId
    status
    dateStart
    dateEnd
    description
    createdAt
    updatedAt
  }
}